# 📅 Daily Scrum - 2026년 7월 31일 (금)

## 👤 작성자
- **이름:** 김가율

---

## 🟢 Yesterday / Today (어제 & 오늘 진행한 작업)
- [o] **개발 환경 세팅 및 프로젝트 기반 구축**
  - `uv` / `requirements.txt` 기반 패키지 의존성 정의 (Django 6.0.7, DRF, LangChain, OpenAI 연동 등)
- [o] **도메인 중심 앱(App) 구조 설계 및 생성**
  - `users` (인증/회원), `chat` (대화 CRUD), `llm_core` (LLM 스트리밍) 3개 앱 분리
- [o] **보안 및 DB 환경 구성**
  - `django-environ` 적용하여 `.env` 기반 비밀 정보(Secret Key, DB URL) 분리
  - SQLite (`DATABASE_URL=sqlite:///db.sqlite3`) 연동 완료
- [o] **데이터베이스 모델링 및 마이그레이션**
  - Custom User Model (`users.User`) 설계 및 `AUTH_USER_MODEL` 설정
  - 대화 세션 및 메시지 ORM 모델(`ChatSession`, `ChatMessage`) 정의 후 초기 마이그레이션 실행 완료

---

## 🟡 Tomorrow (내일 진행할 작업)
- [ ] **CORS & Security 설정**
  - `django-cors-headers` 미들웨어 연동 및 프론트엔드(`localhost:3000/5173`) 출처 허용
  - `REST_FRAMEWORK` 기본 인증 클래스(JWT) 세팅
- [ ] **`users` 앱 API 개발**
  - JWT 기반 회원가입 및 로그인 REST API 구현 (`djangorestframework-simplejwt`)
- [ ] **API 문서화 기본 구성**
  - `drf-spectacular` 연결하여 Swagger UI 접근 확인

---

## 🔴 Blockers / Issues (이슈 및 해결)
- **이슈:** `python manage.py migrate` 실행 시 `ImproperlyConfigured` (ENGINE 설정 누락) 에러 발생
- **해결:** `settings.py` 내 `django-environ` 연동 구문 보완 및 `.env` 파일의 `DATABASE_URL` 구문 (`sqlite:///db.sqlite3`) 정상 적용으로 해결 완료