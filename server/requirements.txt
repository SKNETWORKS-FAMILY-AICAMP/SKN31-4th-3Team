# -----------------------------------------------------------------------------
# Django/DRF, JWT 인증, LLM 연동(LangChain/OpenAI), 비동기/스트리밍 및 DB 연결 라이브러리들을 모듈별로 정리
# Core Framework & API
# -----------------------------------------------------------------------------
# ★ 원래 Django==6.0.7 로 고정돼 있었으나 그 버전은 아직 배포되지 않았다.
#   pip install 이 바로 실패해 Docker 빌드가 첫 줄에서 멈춘다.
#   현재 LTS 인 5.2 로 내렸다 — 쓰는 기능은 모두 5.2 에 있다.
Django>=5.2,<6.0
djangorestframework>=3.15.0
django-cors-headers>=4.3.1
django-environ>=0.11.0

# -----------------------------------------------------------------------------
# Authentication & Security
# -----------------------------------------------------------------------------
djangorestframework-simplejwt>=5.3.1
PyJWT>=2.8.0

# -----------------------------------------------------------------------------
# Database & ORM
# -----------------------------------------------------------------------------
psycopg2-binary>=2.9.9

# 벡터 검색. Django 필드 + 인덱스 + 거리 연산자를 ORM 으로 다룬다.
#
# ★ SQLite 에서는 벡터 컬럼을 쓸 수 없다.
#   확장이 Postgres 전용이다. 그래서 임베딩을 쓰는 코드 경로는 전부
#   "없으면 키워드 검색으로 폴백" 으로 짠다 — 팀원이 DB 없이 clone 만
#   해도 앱이 돌아야 한다.
pgvector>=0.3.0

# -----------------------------------------------------------------------------
# LLM & AI Integration
# -----------------------------------------------------------------------------
# LangChain 으로 프롬프트·히스토리·스트리밍을 조립한다 (llm_core/chains.py).
#
# ★ langchain 메타패키지를 넣지 않는다.
#   그 패키지는 쓰지도 않는 커뮤니티 통합을 잔뜩 끌고 온다.
#   필요한 두 개만 명시한다.
#
# ★ 예전 자료의 `from langchain.chat_models import ChatOpenAI` 는 폐기됐다.
#   지금은 `from langchain_openai import ChatOpenAI` 다.
langchain-core>=1.0
langchain-openai>=1.0

# ChatOpenAI 가 내부적으로 쓴다. 버전 충돌을 막기 위해 명시해 둔다.
openai>=1.30.0

# -----------------------------------------------------------------------------
# Graph (Neo4j Aura)
# -----------------------------------------------------------------------------
# 감정·인물·사건 그래프. 벡터가 "말이 비슷한 것" 을 찾는다면 이쪽은
# "이어져 있는 것" 을 찾는다 (scripture/graph.py).
#
# ★ 공식 드라이버만 넣는다.
#   langchain-neo4j 같은 래퍼도 있지만, 우리가 쓰는 것은 Cypher 한두
#   개다. 래퍼를 얹으면 스키마가 바뀔 때마다 래퍼의 규약까지 함께
#   맞춰야 한다.
neo4j>=5.23.0

# -----------------------------------------------------------------------------
# Async, Caching & Task Queue
# -----------------------------------------------------------------------------
# ★ 아직 쓰지 않는다. 스트리밍·비동기 작업을 붙일 때 주석을 푼다.
#   설치만 해 두면 이미지가 커지고 취약점 스캔 대상만 늘어난다.
# redis>=5.0.4
# celery>=5.4.0

# -----------------------------------------------------------------------------
# API Documentation & Open API Schema
# -----------------------------------------------------------------------------
drf-spectacular>=0.27.2

# -----------------------------------------------------------------------------
# Testing & Quality
# -----------------------------------------------------------------------------
pytest>=8.2.0
pytest-django>=4.8.0
pytest-asyncio>=0.23.6

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
gunicorn>=22.0.0
uvicorn>=0.30.0