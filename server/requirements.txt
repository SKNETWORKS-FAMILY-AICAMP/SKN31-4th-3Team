# -----------------------------------------------------------------------------
# Django/DRF, JWT 인증, LLM 연동(LangChain/OpenAI), 비동기/스트리밍 및 DB 연결 라이브러리들을 모듈별로 정리
# Core Framework & API
# -----------------------------------------------------------------------------
# ★ 원래 Django==6.0.7 로 고정돼 있었으나 그 버전은 아직 배포되지 않았다.
#   pip install 이 바로 실패해 Docker 빌드가 첫 줄에서 멈춘다.
#   현재 LTS 인 5.2 로 내렸다 — 쓰는 기능은 모두 5.2 에 있다.
Django>=5.2,<6.0
djangorestframework>=3.15.0
django-cors-headers>=4.3.1
django-environ>=0.11.0

# -----------------------------------------------------------------------------
# Authentication & Security
# -----------------------------------------------------------------------------
djangorestframework-simplejwt>=5.3.1
PyJWT>=2.8.0

# -----------------------------------------------------------------------------
# Database & ORM
# -----------------------------------------------------------------------------
psycopg2-binary>=2.9.9

# -----------------------------------------------------------------------------
# LLM & AI Integration
# -----------------------------------------------------------------------------
# ★ 아직 호출하지 않는다 (chat/guide.py 가 템플릿으로 답한다).
#   연동 시점에 주석을 풀고 llm_core/services.py 를 chat/guide.py 의
#   reply() 에 연결한다.
openai>=1.30.0

# -----------------------------------------------------------------------------
# Async, Caching & Task Queue
# -----------------------------------------------------------------------------
# ★ 아직 쓰지 않는다. 스트리밍·비동기 작업을 붙일 때 주석을 푼다.
#   설치만 해 두면 이미지가 커지고 취약점 스캔 대상만 늘어난다.
# redis>=5.0.4
# celery>=5.4.0

# -----------------------------------------------------------------------------
# API Documentation & Open API Schema
# -----------------------------------------------------------------------------
drf-spectacular>=0.27.2

# -----------------------------------------------------------------------------
# Testing & Quality
# -----------------------------------------------------------------------------
pytest>=8.2.0
pytest-django>=4.8.0
pytest-asyncio>=0.23.6

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
gunicorn>=22.0.0
uvicorn>=0.30.0