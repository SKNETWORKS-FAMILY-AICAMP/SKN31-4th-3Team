# Eden — 백엔드 코드 변경 대조

작성 시점: 2026-08-05
기준: 팀이 보내 준 원본 파일 → 지금 저장소

각 항목은 **왜 → 무엇을 → 전/후 코드** 순입니다.
`08-백엔드-변경-보고서.md` 가 요약이라면, 이 문서는 실제 코드입니다.

---

## 목차

| # | 파일 | 성격 |
|---|---|---|
| 1 | `llm_core/services.py` | **수정** — 서버가 안 뜨던 문제 |
| 2 | `llm_core/negotiation.py` | **신규** — 브라우저에서만 실패하던 문제 |
| 3 | `chat/views.py` | **수정** — 2번을 연결 |
| 4 | `users/models.py` | **수정** — 이름 중복 가입 불가 |
| 5 | `users/serializers.py` | **수정** — PATCH 가 조용히 실패 |
| 6 | `config/settings.py` | **수정** — 운영 설정 보존 |
| 7 | `scripture/migrations/0002_…` | **신규** — migrate 만으로 데이터까지 |
| 8 | `admin.py` 3개 | **신규** — DB 확인 경로 |
| 9 | `users/…/dbcheck.py` | **신규** — DB 상태 한눈에 |

`chat/urls.py` 는 **팀 파일을 그대로 씁니다** (변경 없음).

---

## 1. `llm_core/services.py` — 키가 없으면 서버가 안 뜬다

### 왜

```python
client = OpenAI(api_key=os.environ.get("OPENAI_API_KEY"))
```

이 줄이 **모듈 최상단**에 있었습니다. `OpenAI(api_key=None)` 은 생성하는 순간
예외를 던집니다.

```
OpenAIError: Missing credentials. Please pass an `api_key` ...
```

이 파일은 `chat/views → chat/urls → config/urls` import 사슬 위에 있어서,
키가 없으면 **Django 자체가 부팅에 실패**합니다. `manage.py check` 가 여기서 죽었습니다.

### 전

```python
import os
from openai import OpenAI
from django.conf import settings

client = OpenAI(api_key=os.environ.get("OPENAI_API_KEY"))   # ← 여기서 터진다

def generate_llm_response(messages_history: list, system_prompt: str = None) -> str:
    formatted_messages = []
    if system_prompt:
        formatted_messages.append({"role": "system", "content": system_prompt})
    else:
        formatted_messages.append({"role": "system", "content": "너는 친절하고 유용한 AI 보조원이야."})
    formatted_messages.extend(messages_history)

    try:
        response = client.chat.completions.create(
            model="gpt-4o-mini",
            messages=formatted_messages,
            temperature=0.7,
        )
        return response.choices[0].message.content
    except Exception as e:
        raise RuntimeError(f"OpenAI API 호출 중 오류가 발생했습니다: {str(e)}")

def generate_llm_stream_response(messages_history):
    client = OpenAI(api_key=settings.OPENAI_API_KEY)
    response = client.chat.completions.create(
        model="gpt-4o-mini",
        messages=messages_history,
        stream=True
    )
    for chunk in response:
        if chunk.choices and chunk.choices[0].delta.content:
            yield chunk.choices[0].delta.content
```

### 후

```python
from django.conf import settings
from openai import OpenAI

#: 기본 모델. 두 함수가 같은 값을 쓰도록 한곳에 둔다.
MODEL = "gpt-4o-mini"
DEFAULT_SYSTEM_PROMPT = "너는 친절하고 유용한 AI 보조원이야."

_client = None


def get_client() -> OpenAI:
    """
    처음 호출될 때 한 번만 만든다.

    키가 없으면 여기서 처음으로 실패한다 — 호출한 화면만 오류를 보고,
    나머지 서비스는 계속 돈다.
    """
    global _client
    if _client is None:
        api_key = getattr(settings, "OPENAI_API_KEY", "") or None
        if not api_key:
            raise RuntimeError(
                "OPENAI_API_KEY 가 설정되지 않았습니다. server/.env 를 확인해 주세요."
            )
        _client = OpenAI(api_key=api_key)
    return _client


def generate_llm_response(messages_history: list, system_prompt: str = None) -> str:
    """이전 대화 내역을 받아 한 번에 답변을 생성한다 (동기 방식)."""
    formatted_messages = [
        {"role": "system", "content": system_prompt or DEFAULT_SYSTEM_PROMPT}
    ]
    formatted_messages.extend(messages_history)

    try:
        response = get_client().chat.completions.create(
            model=MODEL,
            messages=formatted_messages,
            temperature=0.7,
        )
        return response.choices[0].message.content
    except Exception as e:
        raise RuntimeError(f"OpenAI API 호출 중 오류가 발생했습니다: {str(e)}")


def generate_llm_stream_response(messages_history):
    """답변을 조각으로 흘려보낸다 (SSE 용). 조각 하나가 곧 yield 하나다."""
    response = get_client().chat.completions.create(
        model=MODEL,
        messages=messages_history,
        stream=True,
    )
    for chunk in response:
        if chunk.choices and chunk.choices[0].delta.content:
            yield chunk.choices[0].delta.content
```

### 바뀐 점

| | 전 | 후 |
|---|---|---|
| 클라이언트 생성 | import 시점 | 첫 호출 시점 |
| 키 없을 때 | **서버 전체 다운** | 그 호출만 실패 |
| 모델명 | 두 곳에 문자열 | 상수 `MODEL` 하나 |
| 시스템 프롬프트 | if/else 4줄 | `or` 한 줄 |

**로직은 그대로입니다.** 언제 만드느냐만 바꿨습니다.

---

## 2. `llm_core/negotiation.py` (신규) — 브라우저에서만 실패한다

### 왜

DRF 는 핸들러를 부르기 **전에** `Accept` 헤더로 렌더러를 고릅니다.
기본은 JSON 뿐이라 브라우저가 SSE 규격대로 `Accept: text/event-stream` 을 보내면
**406 Not Acceptable** 로 막힙니다. `post()` 는 실행조차 안 됩니다.

나란히 재현한 결과입니다.

```
① Accept 없음                 → 200
② Accept: text/event-stream  → 406 "요청 Accept 헤더를 만족시킬 수 없습니다."
```

**Accept 헤더 없이 부르면 통과하므로 curl · Swagger · 테스트 클라이언트로는 멀쩡합니다.**
브라우저에서만 실패해서 원인을 짚기 어려웠습니다.

### 전체 코드 (신규)

```python
from rest_framework.negotiation import BaseContentNegotiation
from rest_framework.renderers import BaseRenderer


class EventStreamRenderer(BaseRenderer):
    """
    형식만 갖춘 렌더러.

    실제로 렌더하지 않는다 — 뷰가 StreamingHttpResponse 를 직접 반환하므로
    이 render() 는 호출되지 않는다. drf-spectacular 가 스키마에
    text/event-stream 을 표시하게 하는 역할도 겸한다.
    """

    media_type = 'text/event-stream'
    format = 'txt'
    charset = 'utf-8'

    def render(self, data, accepted_media_type=None, renderer_context=None):
        return data


class IgnoreClientContentNegotiation(BaseContentNegotiation):
    """Accept 헤더를 따지지 않고 첫 번째 렌더러를 쓴다."""

    def select_parser(self, request, parsers):
        return parsers[0]

    def select_renderer(self, request, renderers, format_suffix=None):
        return (renderers[0], renderers[0].media_type)
```

> **프론트에서 Accept 헤더를 빼는 방법도 있었지만 택하지 않았습니다.**
> 그건 "규격대로 보내면 깨지는 API" 를 남기는 것입니다.
> 다음 사람이 표준대로 짜면 또 막힙니다.

---

## 3. `chat/views.py` — 2번을 연결

**변경은 3줄뿐입니다.** 나머지는 팀 코드 그대로입니다.

```diff
  from drf_spectacular.utils import extend_schema
+ from llm_core.negotiation import EventStreamRenderer, IgnoreClientContentNegotiation

  class ChatStreamView(APIView):
      """
      POST /api/v1/chat/sessions/<session_id>/stream/
      """
      permission_classes = [permissions.IsAuthenticated]
+
+     # ★ Accept: text/event-stream 을 406 으로 막지 않는다 (llm_core/negotiation.py)
+     renderer_classes = [EventStreamRenderer]
+     content_negotiation_class = IgnoreClientContentNegotiation
```

### 확인

```
Accept 없음                    200  text/event-stream
Accept: text/event-stream      200  text/event-stream
```

---

## 4. `users/models.py` — 같은 이름으로 두 번째 사람이 가입 못 한다

### 왜

`AbstractUser` 의 `username` 은 기본이 `unique=True` 입니다.
로그인 ID 로 쓸 때는 맞지만, 우리는 `USERNAME_FIELD` 가 email 이고
이 값은 **"○○님, 어서 오세요"** 로 부르는 호칭일 뿐입니다.

```
1번째 "지훈" 가입: 201
2번째 "지훈" 가입: 400 {"username":["해당 사용자 이름은 이미 존재합니다."]}
```

발표 자리에서 두 명이 시연하면 바로 터졌을 겁니다.

### 전

```python
class User(AbstractUser):
    email = models.EmailField(unique=True)
    mbti = models.CharField(max_length=4, null=False, blank=False)

    USERNAME_FIELD = 'email'
    REQUIRED_FIELDS = ['username', 'mbti']
```

### 후

```python
class User(AbstractUser):
    email = models.EmailField(unique=True)

    #: 필수. 유효성 검사는 serializer 한 곳에서만 한다.
    mbti = models.CharField(max_length=4, null=False, blank=False)

    """
    ★ 이름은 겹쳐도 된다.
      AbstractUser 의 username 은 기본적으로 unique 다. 로그인 ID 로 쓸 때는
      맞는 설계지만, 우리는 USERNAME_FIELD 가 email 이고 이 값은 화면에서
      "○○님, 어서 오세요" 로 부르는 호칭일 뿐이다.

      unique 인 채로 두면 지훈이라는 사람 둘 중 한 명은
      "해당 사용자 이름은 이미 존재합니다" 를 보고 가입을 포기한다.
    """
    username = models.CharField(max_length=150, unique=False)

    USERNAME_FIELD = "email"
    REQUIRED_FIELDS = ["username", "mbti"]
```

**마이그레이션** `users/0004_alter_user_username.py`

### 확인

```
1번째 "지훈" 가입: 201
2번째 "지훈" 가입: 201   ← 고침
```

---

## 5. `users/serializers.py` — PATCH 가 조용히 실패한다

### 왜

받은 파일에는 `UserUpdateSerializer` 가 없고, `UserSerializer` 는
`read_only_fields = fields` 입니다. 그대로 PATCH 에 쓰면
**200 을 돌려주면서 값은 바뀌지 않습니다.**
화면에서는 저장된 것처럼 보이고 새로고침하면 되돌아갑니다.
가장 찾기 어려운 종류의 버그입니다.

프론트의 오른쪽 MBTI 목록에서 유형을 고르면 이 엔드포인트로 저장됩니다.

### 전

```python
VALID_MBTI = [ ... 16개 ... ]

class RegisterSerializer(serializers.ModelSerializer):
    password = serializers.CharField(write_only=True, min_length=8)
    mbti = serializers.CharField(required=True, allow_blank=False, max_length=4)

    class Meta:
        model = User
        fields = ('id', 'email', 'username', 'password', 'mbti')

    def validate_mbti(self, value):
        mbti_upper = value.upper()
        if mbti_upper not in VALID_MBTI:
            raise serializers.ValidationError("올바른 MBTI 유형(예: INFP, ENTP 등)을 입력해 주세요.")
        return mbti_upper

    def create(self, validated_data):
        user = User.objects.create_user(...)
        return user


class UserSerializer(serializers.ModelSerializer):
    class Meta:
        model = User
        fields = ('id', 'email', 'username', 'mbti', 'date_joined')
        read_only_fields = fields
```

### 후 — 달라진 부분만

```python
def normalize_mbti(value):
    """
    소문자로 적어도 받아 주고, 16유형이 아니면 거절한다.

    ★ 판정은 여기 한 곳에서만 한다.
      모델 choices 에도 같은 목록을 두면 대문자 변환이 일어나기 전에
      모델 검증이 걸려, 사용자가 맞게 적은 "infp" 가 거부된다.
    """
    upper = value.strip().upper()          # ← strip 추가 (앞뒤 공백)
    if upper not in VALID_MBTI:
        raise serializers.ValidationError('올바른 MBTI 유형(예: INFP, ENTP 등)을 입력해 주세요.')
    return upper


class RegisterSerializer(serializers.ModelSerializer):
    ...
    def validate_mbti(self, value):
        return normalize_mbti(value)       # ← 공용 함수로


class UserUpdateSerializer(serializers.ModelSerializer):     # ← 되살림
    """
    본인 정보 수정. 지금은 MBTI 만 바꿀 수 있다.

    ★ 조회용과 나눠 두는 이유
      UserSerializer 는 모든 필드가 읽기 전용이다. 그걸 그대로 PATCH 에
      쓰면 요청은 200 을 돌려주는데 값은 바뀌지 않는다 — 화면에서는
      저장된 것처럼 보이고 새로고침하면 되돌아간다.
    """

    class Meta:
        model = User
        fields = ('mbti',)

    def validate_mbti(self, value):
        return normalize_mbti(value)
```

`users/views.py` 는 이 두 serializer 를 메서드로 가릅니다 (기존 그대로).

```python
def get_serializer_class(self):
    return UserUpdateSerializer if self.request.method in ("PUT", "PATCH") else UserSerializer
```

### 확인

```
PATCH /api/v1/auth/me/  {"mbti": "entp"}   →  200  {"mbti": "ENTP"}   실제로 변경됨
```

---

## 6. `config/settings.py` — 운영 설정 보존

받은 파일을 **그대로 덮지 않았습니다.** 팀 파일에서 추가된 것만 얹고,
없으면 고장 나는 항목은 유지했습니다.

### 팀 파일에서 가져온 것

```diff
+ OPENAI_API_KEY = env('OPENAI_API_KEY', default='')

  CORS_ALLOWED_ORIGINS = [
+     "http://localhost:3000",   # CRA / Next 개발 서버
+     "http://127.0.0.1:3000",
      "http://localhost:5173",   # Vite 개발 서버
```

### 유지한 것 (받은 파일에는 없음)

```diff
- SECRET_KEY = env('SECRET_KEY')
+ SECRET_KEY = env('SECRET_KEY', default='dev-only-insecure-key-change-me')

- ALLOWED_HOSTS = []
+ ALLOWED_HOSTS = env.list('ALLOWED_HOSTS', default=['localhost', '127.0.0.1'])
+ SECURE_PROXY_SSL_HEADER = ('HTTP_X_FORWARDED_PROTO', 'https')

  INSTALLED_APPS = [
      'users', 'chat', 'llm_core',
+     'scripture',
  ]

- 'default': env.db('DATABASE_URL')
+ 'default': env.db('DATABASE_URL', default=f'sqlite:///{BASE_DIR / "db.sqlite3"}')

+ DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'
+ STATIC_ROOT = BASE_DIR / 'staticfiles'
+ CSRF_TRUSTED_ORIGINS = env.list('CSRF_TRUSTED_ORIGINS', default=[])

  REST_FRAMEWORK = {
+     'DEFAULT_PAGINATION_CLASS': 'rest_framework.pagination.PageNumberPagination',
+     'PAGE_SIZE': 50,
+     'DEFAULT_THROTTLE_CLASSES': (
+         'rest_framework.throttling.AnonRateThrottle',
+         'rest_framework.throttling.UserRateThrottle',
+     ),
+     'DEFAULT_THROTTLE_RATES': {'anon': '60/min', 'user': '120/min'},
  }
```

### 각 항목이 없으면

| 항목 | 없으면 |
|---|---|
| `scripture` 앱 | **은하 · 구절 API 전멸** (프론트가 호출 중) |
| `SECRET_KEY` 기본값 | `.env` 없으면 `git clone` 후 즉시 크래시 |
| `DATABASE_URL` SQLite 폴백 | 위와 같음 |
| `ALLOWED_HOSTS` env | `DEBUG=False` 로 켜면 전 요청 400 |
| `STATIC_ROOT` | `collectstatic` 실패 |
| `DEFAULT_AUTO_FIELD` | 매 `makemigrations` 마다 W042 경고 |
| Throttle | API 남용 제한 없음 |
| `SECURE_PROXY_SSL_HEADER` | ALB 뒤에서 HTTPS 인식 실패 |

---

## 7. `scripture/migrations/0002_seed_scripture_data.py` (신규)

### 왜

`migrate` 는 **표만 만들고 데이터는 넣지 않습니다.**
DB 를 초기화하고 `migrate` 만 돌리면 표는 생기는데 안이 비어,
화면에서 별을 눌렀을 때 **"찾을 수 없는 별입니다"** 가 뜹니다.

명령 하나를 잊어서 생기는 문제가 아니라 **잊을 수 있게 만들어 둔 설계**의
문제였습니다. 실제로 DB 를 초기화할 때마다 밟았습니다.

### 핵심 코드

```python
def seed(apps, schema_editor):
    """
    ★ 모델을 직접 import 하지 않고 apps.get_model 을 쓴다.
      마이그레이션은 "그 시점의 모델" 로 돌아야 한다. 나중에 필드가
      바뀌면 직접 import 한 모델과 어긋나 과거 마이그레이션이 깨진다.
    """
    Galaxy = apps.get_model("scripture", "Galaxy")
    Verse = apps.get_model("scripture", "Verse")

    for row in load("galaxies.json"):
        Galaxy.objects.update_or_create(id=row["id"], defaults={...})

    for row in load("verses.json"):
        Verse.objects.update_or_create(id=row["id"], defaults={...})


def unseed(apps, schema_editor):
    """
    되감아도 지우지 않는다.

    구절을 삭제하면 그 구절을 참조하던 대화(seed_verse)가 함께 끊긴다.
    마이그레이션을 되감는 것과 사용자 데이터를 버리는 것은 다른 일이다.
    """
    return


class Migration(migrations.Migration):
    dependencies = [("scripture", "0001_initial")]
    operations = [migrations.RunPython(seed, unseed)]
```

### 확인 — 빈 DB 에 `migrate` 만

```
Applying scripture.0002_seed_scripture_data... OK
→ 은하 13 / 구절 702   (seed 명령 없이)
```

`seed_scripture` 명령도 남겨 뒀습니다. 프론트 데이터를 고친 뒤 DB 만 갱신할 때 씁니다.

---

## 8. `admin.py` 3개 (신규)

### 왜

`users/admin.py`, `chat/admin.py`, `scripture/admin.py` 가 **전부 비어 있었습니다.**

```python
from django.contrib import admin

# Register your models here.
```

관리자 화면에 아무것도 안 보여서 "가입이 DB 에 저장됐는지" 확인할 방법이
없었습니다 — Swagger 는 API 문서일 뿐 DB 를 보여 주지 않습니다.

### `users/admin.py` 요지

```python
@admin.register(User)
class UserAdmin(BaseUserAdmin):
    """
    기본 UserAdmin 은 username 기준으로 정렬·검색한다.
    우리는 email 이 로그인 ID 이므로 그쪽으로 맞춘다.
    """
    list_display = ('email', 'username', 'mbti', 'is_staff', 'date_joined')
    list_filter = ('mbti', 'is_staff', 'is_active')
    search_fields = ('email', 'username')
    ordering = ('-date_joined',)
    fieldsets = (
        (None, {'fields': ('email', 'password')}),
        ('개인 정보', {'fields': ('username', 'mbti')}),
        ...
    )
```

### `chat/admin.py` 요지

```python
class ChatMessageInline(admin.TabularInline):
    """
    대화방을 열면 주고받은 말이 그 안에 보인다.
    LLM 답변이 제대로 저장됐는지 확인할 때 이게 가장 빠르다.
    """
    model = ChatMessage
    extra = 0
```

### 쓰는 법

```bash
python manage.py createsuperuser     # 한 번만
# → http://localhost:8000/admin/
```

---

## 9. `users/management/commands/dbcheck.py` (신규)

터미널에서 DB 상태를 한 번에 봅니다.

```bash
python manage.py dbcheck          # 요약
python manage.py dbcheck --full   # 회원·대화 목록까지
```

```
연결된 DB   sqlite3  ·  db.sqlite3
────────────────────────────────────────────
  회원            11
  대화방          14
  메시지           8
  은하           13
  구절          702
```

은하가 0이면 이렇게 알려 줍니다.

```python
if Galaxy.objects.count() == 0:
    self.stdout.write(self.style.WARNING(
        '\n  ⚠ 은하·구절이 비어 있습니다 → python manage.py seed_scripture'))
```

---

## 10. `tests/test_integration.py` — 21 → 25개

기존 6개가 병합 전 계약(`/chat/threads/`, MBTI 선택)을 보고 있어 갱신했습니다.

### 새로 넣은 것 중 가장 중요한 하나

```python
def test_stream_accepts_event_stream_header(self, auth, seeded, db):
    """
    ★ 브라우저는 SSE 규격대로 Accept: text/event-stream 을 보낸다.

    DRF 는 핸들러를 부르기 전에 Accept 로 렌더러를 고르고, 맞는 것이
    없으면 406 으로 막는다. 실제로 이것 때문에 화면에서만 대화가
    실패했다 — 헤더 없이 부르는 테스트로는 잡히지 않는다.
    """
    sid = auth.post("/api/v1/chat/sessions/", {}, format="json").data["id"]
    response = auth.post(
        f"/api/v1/chat/sessions/{sid}/stream/",
        {"message": "안녕하세요"},
        format="json",
        HTTP_ACCEPT="text/event-stream",       # ← 이 줄이 없어서 놓쳤다
    )
    assert response.status_code == 200
    assert response.headers["Content-Type"].startswith("text/event-stream")
```

### 그 외

- 스트림이 실패해도 사용자 메시지는 남는가
- MBTI 소문자 입력 → 대문자 저장
- MBTI 없이 가입 → 400
- 같은 이름으로 두 명 가입 → 둘 다 201

---

## 부록 — 파일별 규모

| 파일 | 성격 | 줄 |
|---|---|---:|
| `llm_core/services.py` | 수정 | 70 |
| `llm_core/negotiation.py` | 신규 | 52 |
| `chat/views.py` | 수정 (3줄) | 214 |
| `chat/urls.py` | 팀 파일 그대로 | 26 |
| `users/models.py` | 수정 | 52 |
| `users/serializers.py` | 수정 | 84 |
| `users/admin.py` | 신규 | 41 |
| `chat/admin.py` | 신규 | 39 |
| `scripture/admin.py` | 신규 | 24 |
| `users/…/dbcheck.py` | 신규 | 69 |
| `scripture/migrations/0002_…` | 신규 | 98 |
| `config/settings.py` | 수정 | 238 |
| `tests/test_integration.py` | 수정 | 294 |
